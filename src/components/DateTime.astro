---
const { datetime } = Astro.props;
---

<time datetime={datetime.toISOString()}
  >{
    datetime.toLocaleDateString(undefined, {
      dateStyle: "medium",
      timeZone: "UTC",
    })
  }</time
>
