---
import Lift from "@components/Lift.astro";
import "../styles/base.css";
// component import
import Footer from "@components/Footer.astro";
import MainHead from "@components/MainHead.astro";
import Nav from "@components/NavHome.astro";
import SkipLinks from "@components/SkipLinks.astro";

const {
  title = "My Astro Site",
  description = "Change values in siteData.json to update this description",
  image,
  frontmatter,
  noRobots,
  classes,
  overrideSiteTemplate = false,
  homeData,
} = Astro.props;
---

<html lang="fr">
  <MainHead
    {title}
    {description}
    {overrideSiteTemplate}
    {image}
    {frontmatter}
    {noRobots}
  />
  <body class="home">
    <SkipLinks />
    <Nav {homeData} />
    <main id="main-content" class:list={["relative flex flex-col", classes]}>
      <slot> Default Text</slot>
      <Lift />
    </main>
    <Footer />
  </body>
</html>
